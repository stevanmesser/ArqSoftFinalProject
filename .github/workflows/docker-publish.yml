name: Docker

on:
  push:
    # Publish `master` as Docker `latest` image.
    branches:
      - master

    # Publish `v1.2.3` tags as releases.
    tags:
      - v*

  # Run tests for any PRs.
  pull_request:

env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: arq-soft-final-project

jobs:
  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build . --file Dockerfile --tag $IMAGE_NAME

      - name: Log into registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          IMAGE_ID=docker.pkg.github.com/${{ github.repository }}/$IMAGE_NAME

          # Change all uppercase to lowercase
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          # Strip git ref prefix from version
          VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')

          # Strip "v" prefix from tag name
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')

          # Use Docker `latest` tag convention
          [ "$VERSION" == "master" ] && VERSION=latest

          echo IMAGE_ID=$IMAGE_ID
          echo VERSION=$VERSION

          docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          docker push $IMAGE_ID:$VERSION
          
      - name: Deploy
        run: |
          curl -X POST http://177-44-248-88.sslip.io:9000/api/webhooks/7a4f0aa8-2a16-4155-9d25-b25daf5fbb2a
          curl -X POST http://177-44-248-88.sslip.io:9000/api/webhooks/f9e16224-9430-4311-baaf-566cf2a4ceef
          curl -X POST http://177-44-248-88.sslip.io:9000/api/webhooks/ec336f9a-39d7-4114-bf80-e831bfc0fb41
          curl -X POST http://177-44-248-88.sslip.io:9000/api/webhooks/55cbd941-81a2-4ce1-85c9-987ce002c3ad
          curl -X POST http://177-44-248-88.sslip.io:9000/api/webhooks/06101dc0-b891-4b1f-8e50-7d42c39eefc1
          curl -X POST http://177-44-248-88.sslip.io:9000/api/webhooks/3d515841-5460-4306-b55c-027f6c063614
